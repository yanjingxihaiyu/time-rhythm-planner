---
name: time-rhythm-planner
description: 通用时间管理 skill——基于用户已验证的周节律与"疲惫即降强度"原则，生成本地周/日计划草稿，确认后推送飞书（日历或多维表格）。当用户说"帮我排一下这周""做个作息表""安排下时间""周计划""日计划""精力怎么排""把训练排进日程""下周怎么排"时触发。
version: 1.0.0
agent_created: true
---

# 精力节律与周计划安排（通用时间管理）

把"想做的事"变成"守得住的周/日节奏"。核心原则：**先尊重精力，再排任务；先本地草稿，再推飞书。**
本 skill 不绑定具体项目，是一个可复用的精力管理 + 排程模板库；用户已知的精力画像与约束作为**默认锚点**写入，每次可改。

> 说明：本文件是「方法模板版」，所有 `[方括号]` 处都是需替换的个性化项。首次使用时，把用户真实的精力节律填进二.3 的锚里即可。原始作者版含其私人日程，本公开版已脱敏，仅保留可复用的结构与逻辑。

## 一、适用场景

- 用户说"帮我排这周 / 做个作息表 / 周计划 / 日计划 / 精力怎么排"。
- 多个承诺撞车（咨询、课程交付、训练、家庭、自我照料），需要落到一周里且不透支。
- 想把长期训练计划（如徒步/马拉松，可放飞书多维表格）或某段项目周期，排成可执行的周节奏。
- 产出先本地 Markdown 迭代，确认后再推飞书。

## 二、前置条件

1. **飞书 CLI 已授权（user 身份）**。若 `lark-cli whoami` 报 `hermes context detected but not bound` 或身份是 bot，先按 `lark-cli-workbuddy-setup` 技能重跑绑定 + 扫码登录。user token 约 **3 周过期**，过期只需 `auth login --recommend` 重新扫码，不用重建应用。
2. **（可选）读已有排期/训练表**：若要从飞书多维表格拉训练计划或已有排期，需知道 `base-token`（多维表格的 flag 是 `--base-token`，不是 `--base-id`）。拉取命令与字段以 `lark-base` 技能为准。
3. **精力画像锚（默认 = 一个示例锚，请替换为你自己的节律）**——本 skill 不绑定具体人物；首次使用时把"用户已知的精力画像与约束"作为**默认锚点**写入，每次可改。下方给一个**结构化模板**（用 `[方括号]` 标出需替换的个性化项），可直接套用并填实；锚的来源真身可以是用户本地的一张「时间安排表.md」，skill 锚为其快照，原表更新后需同步回此处。
   - **每日晨间固定流程（工作日）**：[你的起床时间] 起 → [晨间固定动作，如家务/运动/冥想] → **写当日计划（≤3 件重要事，可落工作台「今天必做的事情 / tasks」或本地小本）** → [你的晨间习惯，如读书]。
   - **周循环骨架（角色分工，能量按角色定）**：按"角色 → 能量特征 → 固定块/适合任务 → 禁排"四列，列出你一周七天各自的主角色。示例骨架（替换为你自己的）：
     | 日 | 角色 | 能量特征 | 固定块 / 适合任务 | 禁排 |
     |---|---|---|---|---|
     | 周一 | [你的高消耗日角色，如训练/攻坚] | [能量特征] | [固定块] | [禁排] |
     | 周二 | [恢复+前置日] | [能量特征] | [温和活动；把本周某类前置一次性做完] | [禁排] |
     | 周三 | [执行日A + 晚间课程] | [能量特征] | [下午执行某类产出；晚 [你的固定课程]] | [禁排] |
     | 周四 | [执行日B] | [能量特征] | 同周三 | 同周三 |
     | 周五 | [咨询/服务日] | [能量特征] | [上午高消耗服务；下午休养+轻量产出] | [禁排] |
     | 周六 | [家庭/轻日 + 晚间课程] | — | [家庭安排，你只需…]；晚 [你的固定课程] | [禁排] |
     | 周日 | [备课/深度日 + 家庭] | [能量特征] | [家庭任务（可能）+ 深度备课/产品打磨；可约工作坊/一对一] | [禁排] |
   - **家庭/孩子任务分布（如适用）**：[描述你与家人/孩子的真实分工——哪些天需操心、哪些天零任务；把"尚未启动但需排进时间"的事项先占一个待定弹性槽，别长期空着]。
   - **固定课程 / 学习时间（自我迭代，不可挤占）**：当前在学 [你的课程名]（用 AI/工具给自己加杠杆），固定 [周X、周X 晚 HH:MM 线上课]；列为不可挤占。后续新课优先占同段或另开固定晚段，**不在高消耗日硬塞深度新课**，且新课须进"学习夜"弹性池统一管理。
   - **日内精力带（叠加在周角色上）**：晨间 ★★★（深度创作/写课/复盘）、上午 ★★（沟通/轻产出）、午后 ☆（事务/休息）、深夜 ★★★（复盘/随笔）。[按你的真实峰值低谷调]
   - **健康闸门（硬性）**：[你的健康约束，如慢性病/过敏/生理期]；**疲惫即降强度不硬扛**。状态差的日子有氧/力量全降档只留温和活动；若落在高消耗日，把高消耗任务前移到身体尚可时段集中完成，虚弱期只做轻量事务不硬扛外出；该窗口不排讲课/重备课。
   - **创作灵感补给（内容创作命脉，不可让位）**：源源不断的选题与课程灵感，来自"外部信息冲击 → 内省消化 → 输出"的飞轮，须作为固定块预留。
     - **① 向外获取（信息冲击·主动喂料）**：每天留一段主动读新闻/看优质内容/看他人作品，让外部信息碰撞大脑。这是接收阶段，性质是"创作喂料"非消遣；建议落在午后低谷后或傍晚恢复段，时长 30–60'。落到工作台「创作灵感补给 · ① 向外获取」区。
     - **② 内省总结（联想/与AI对话/提炼）**：把接收到的东西安静地做联想、提炼成自己的观点；可先与 AI 对话碰撞、再回来总结成选题/课程灵感。两个天然窗口：晨间读书后延伸 15–30' + 深夜复盘段。落到工作台「创作灵感补给 · ② 内省总结」区。
     - **两步顺序不可颠倒**：先①向外获取，再②内省总结；没有输入就逼自己总结往往空转。两步都进工作台 `ideas` 域（用 `stage:'input'`/`stage:'digest'` 区分）。
     - **归位规则**：工作台 `ideas` 只是**当日轻量采集收件箱**（快记、带日期、走获取→总结两步），不做深度沉淀；**[你的知识库（如 Obsidian）]才是灵感的 source of truth 与长期档案**。流程：工作台当日采集 → **周复盘把好的 distill 进知识库**变真素材。**单向流动、不双写**。
     - **闭环落到选题库**：② 内省总结产出的灵感随手进你的选题库"延展/灵感池"，选选题时优先消化，形成"获取→总结→选题→拍→反馈→再获取"的创作飞轮。
     - **铁律**：信息冲击与内省消化块和睡眠/家庭时间同级，不可挤占；疲惫日可缩短但不取消（灵感枯竭比少拍一条更伤长期产能）。
   - **短视频固定节奏（如适用）**：每周 [N] 次外景 = [周X、周X 下午]；前置全在 [前置日]；剪辑分散 [周X/周日] 碎片；保底 [N] 条，状态好冲 [N+1]；拍摄绝不顺延周末。
   - **发布平台（均为视频形式，非图文）**：[你的平台，如抖音/视频号/小红书]。工作台「内容复盘」模块按你实际平台配置，排计划写脚本时默认这些视频平台。
   - **工作坊/训练营（候选窗口）**：[你的产品/训练候选窗口]，严格避开高消耗日与低谷窗口。深度备课窗口 = [你的备课日]。
   - **一对一/新产品**：物料（海报/文案）放 [前置日] 空余上架；预约窗口 = [你的可用时段]；避开高消耗/低谷。
   - **兜底**：[你的天气/突发兜底，如坏天切上午拍摄、被打断合并拍完、绝不顺延周末]。
   - **不可挤占块**：晨间固定流程、睡眠下限、[家庭时间块]、[固定课程晚段]、创作补给块、每周至少 1 个完整休息半日。[按你真实固定块列]
   - **节奏认知**：[你最饱满的月份/阶段] 预留弹性，不追求每周满分。

4. **短视频生产流水线（默认锚，8 步闭环）**——用户做短视频的固定工序；选题库可放在你常用的知识库（飞书/Notion/本地 md 等），建议单开一个《选题》文档持续积累。每步落到周节律的时段如下：

   | 步 | 动作 | 落到周节律 | 说明 |
   |---|---|---|---|
   | 1 找选题 | 从选题库挑本周 N 条 | [前置日]（或随时积累进池）| 前 N 条优先 |
   | 2 选配套衣服 | 按选题挑 N 套出镜 + 配饰，标记需熨烫 | [前置日] | 与选题绑定，不临时找 |
   | 3 写文案 | 用完口播文案 | [前置日] | 拍摄当天只执行，不构思 |
   | 4 选时拍摄 | 定 [拍摄日] 时段 | 锚定 [拍摄日下午] | 坏天/暴晒 → 切上午 |
   | 5 拍摄 | 熨烫 → 化妆 → 实拍 | [拍摄日下午] | 当天只执行；保底 N 条、状态好冲 N+1 |
   | 6 剪辑 | 粗剪精修 | [剪辑日] 碎片 | 拍完不必立刻剪 |
   | 7 发布 | 上线 | 剪辑后（通常当周）| — |
   | 8 数据反馈 | 看播放/互动/转化，反哺下期 | 发布后 → 回步骤 1 | 闭环：反馈进下期选题 |

   - **铁律**：步骤 1–3（选题/衣服/文案）全部压在**前置日**完成；拍摄当天只做执行（熨烫-化妆-出门实拍），绝不现场构思；剪辑分散碎片日；拍摄**绝不顺延周末**。
   - **闭环**：第 8 步数据反馈直接喂回第 1 步选题，形成"选题→衣服→文案→拍摄→剪辑→发布→反馈→再选题"的周循环。
   - **禁词范围澄清（如适用）**：选题库草稿里若出现你所在某平台的「一票否决词」，那**仅**是你往该平台提交海报/展览物料时的限制（你曾因类目重叠被退稿）。**你自己出镜的短视频、直播不存在此禁忌**，选题库表述可直接用；只有拿去该平台提交的物料才需跑对应禁词清单。

## 三、工作流程（7 步）

1. **收输入**：用户给的承诺清单（咨询场次、课程交付节点、训练日、家庭安排、行政杂事），以及本周特殊项（出差、过敏高发期、deadline）。缺信息先问清"这周必须钉死的有哪几件 + 哪几天状态会差"，不要凭空编。
2. **定节律**：用第四节"精力节律模板"，按用户确认的峰值/低谷分段（默认锚见上）。每段标注"适合的任务类型"。
3. **排周计划**：按下列**排程原则**把任务填进节律格，产出本地草稿（第四节周计划模板）。
4. **写本地底稿**：落到 `[你的本地知识库路径]/时间安排/`（该目录不存在则先建），文件名 `周计划_YYYY-Www.md`（如 `周计划_2026-W36.md`）。这是源真身与备份。
5. **交付本地草稿给用户迭代**：把草稿要点回读给用户，问"哪块要调"，改到确认。
6. **（确认后）推飞书**：见第五节。二选一或都做——日历（时间块提醒）或/和多维表格（可追踪表）。
7. **验证 + 留白**：日历用 `lark-cli calendar +agenda` 抽查；多维表格用对应 list 命令确认。周复盘栏留白，提醒用户周末回填。

### 排程原则（核心算法，必须遵循）

1. **周角色先行**：先按锚（二.3 周循环骨架）定死每周各日角色，再把本周**可变任务**放进对应日的"弹性/可变槽"，**不要在拍摄日硬塞深度备课、不在高消耗日排重脑力**。
2. **先填精力锚，再放任务**：一天分 晨间高峰/上午平稳/午后低谷/傍晚恢复/深夜高峰（锚已给默认值，可改），每段标适合类型。
3. **任务按消耗类型分级**：深度创作（写课、复盘、长文、产品设计、大纲打磨）→ 峰值段；互动沟通（咨询、直播、回复、会议、答疑）→ 平稳/恢复段；事务执行（行政、排版、跑腿、前置准备）→ 低谷段。
4. **深度任务不进低谷**：午后只排事务或休息；拍摄日午后已被拍摄占。
5. **每天留白**：至少 1 段无排程缓冲；周至少 1 个完整休息半日，写死不可被任务挤占。
6. **健康闸门（硬性）**：若某天标"疲惫/过敏发作/状态差/[低能量窗口]"，自动把该天高强度任务降级或平移到峰值日，**不排训练、不排讲课/个案/重备课**，并在草稿用 ⚠️ 标注；该窗口落在高消耗日则把高消耗任务前移集中完成。
7. **边界优先**：晨间固定流程、睡眠下限、[家庭时间块]、[固定课程晚段]、创作补给块、休息半日作为固定块先占格，剩余空间再排任务。
8. **少而稳**：一周钉死 3–5 件要事即可，其余进"弹性池"，不把格子排满导致守不住；最饱满的阶段预留弹性，不追求每周满分。
9. **创作补给不可让位**：灵感靠"外部冲击 + 内省消化"持续喂料。排程时把信息冲击块与内省消化块当作和睡眠/训练同级的固定块先占格；疲惫日可缩短但不可整段取消。

## 四、模板（必用）

### 4.1 精力节律模板（套用锚生成，本周变量填尖括号）

先和用户确认本周特殊项：[低能量窗口日期] / [产品是否已定开课日] / [家庭额外安排] / [出差或过敏高发]。然后套下方周骨架（锚已验证，通常不变；变的是尖括号里的具体项）。

```
周骨架（默认锚 · 示例模板，请替换为你自己的）
┌──────┬────────────┬──────────────────────┬────────────────────────────┬──────────────┐
│ 日    │ 角色        │ 晨间固定              │ 核心块                      │ 弹性/可变项    │
├──────┼────────────┼──────────────────────┼────────────────────────────┼──────────────┤
│ 周一  │ [高消耗日]  │ [晨间流程]            │ [固定块，如训练/攻坚]        │ <本周变量>    │
│ 周二  │ [恢复+前置] │ 同上                  │ [温和活动；某类前置全做]      │ <进度>        │
│ 周三  │ [执行日+晚课]│ 同上                 │ [下午执行产出；晚 [固定课程]] │ <状态差→前移>│
│ 周四  │ [执行日]    │ 同上                  │ 同周三                      │ 同上          │
│ 周五  │ [咨询/服务] │ 同上                  │ [上午服务；下午休养+轻量产出] │ <场次>        │
│ 周六  │ [家庭/轻日+晚课]│ —               │ [家庭安排；晚 [固定课程]]     │ 当日自由      │
│ 周日  │ [备课+家庭] │ —                    │ [家庭任务+深度备课；可约工作坊/一对一] │ <开课日?> │
└──────┴────────────┴──────────────────────┴────────────────────────────┴──────────────┘

日内精力带（叠加）：晨间★★★深度 / 上午★★沟通 / 午后☆事务 / 深夜★★★复盘。
⚠️ 健康闸门：标疲惫/过敏/[低能量窗口](<日期>)的日子，深度任务与训练自动降级，已在草稿标 ⚠️。
```

### 4.2 周计划模板（本地草稿主体）

```
# 周计划_YYYY-Www（MM/DD–MM/DD）
> 精力锚：见 4.1 ｜ 生成：YYYY-MM-DD ｜ 状态：草稿待确认

## 本周要事（钉死 3–5 件）
1. <要事 + 截止 + 落在哪天哪个时段>
2. ...
弹性池：<可顺延、不占死格子的任务>

## 每日节奏
### 一（MM/DD）
- 晨间 ★★★：<深度创作>
- 上午 ★★：<互动/平稳>
- 午后 ☆：<事务/休息>
- 傍晚 ★：<轻度互动>
   - 深夜 ★★★：<复盘/随笔 + 内省消化/灵感整理>
   - 创作补给：<信息冲击:看了什么/碰撞出什么> ｜ <内省:记下的灵感>
⚠️ 状态备注：<若有疲惫/过敏，写这里，当天强度已降级；创作补给可缩短不可取消>

### 二 … 日（同上结构）

## 训练整合（如有）
- 本周训练日：<周X / 周X> ｜ 阶段：Y1基础/Y2时长/Y3验证
- 训练后恢复缓冲：<排了哪段>
- 与疲惫日冲突检查：<无冲突 / 已平移>

## 周复盘（留白，周末回填）
- 这周节奏守住了吗？哪天崩了、为什么：______
- 精力锚准不准？哪段估错了：______
- 下周要改的 1 件事：______
```

### 4.3 日计划模板（用户只要排一天时用）

```
# 日计划_MM/DD（周X）
> 精力锚：晨间★★★ / 午后☆ / 深夜★★★ ｜ 状态：

## 钉死（≤3 件）
- <事> @<时段> ｜ 消耗类型：深度/互动/事务

## 弹性
- <事>（不占死，有空再做）

## 健康闸门
- 今日状态：正常 / ⚠️疲惫 / ⚠️过敏 → 深度任务已降级/平移至：<时段>

## 不可挤占
- 睡眠下限：__:__ 前睡 ｜ 家庭时间：<段> ｜ 休息半日：<如有>
```

### 4.4 本周短视频流水线清单（套用即填）

```
本周选题（N 条，来自选题库；保底 N、状态好冲 N+1）：
- 条1：<选题> ｜ 配套衣服：<套装A+配饰> ｜ 文案：<写完?/草稿> ｜ 拍摄：[拍摄日] ｜ 剪辑：[剪辑日] ｜ 发布：<日期> ｜ 反馈：<数据>
- 条2：<选题> ｜ 配套衣服：<套装B+配饰> ｜ 文案：<写完?/草稿> ｜ 拍摄：[拍摄日] ｜ 剪辑：[剪辑日] ｜ 发布：<日期> ｜ 反馈：<数据>
（状态好可加条3：<选题>）

前置日检查（拍摄前必须全绿）：
□ 选题定 □ 衣服挑好+熨烫标记 □ 文案写完 □ 设备检查
拍摄日只执行（不构思）：□ 熨烫 □ 化妆 □ 实拍
发布后回填：□ 数据反馈已看 □ 反哺下期选题 <写了哪条/调了什么>
```

### 4.5 本周创作补给清单（内容创作灵感飞轮）

```
【外部信息冲击】每天 30–60'，午后/傍晚；新闻/视频/他人优质内容——主动碰撞，非消遣刷手机
- <周一> 看了：<来源> → 碰撞出：<角度/灵感>
- <周二> 看了：<来源> → 碰撞出：<角度/灵感>
- <周三> …（拍摄日可缩短但不取消）
- <周四> …
- <周五> …
- <周六/日> …

【内省消化】
- 晨间读书后灵感（随手记）：<>
- 深夜复盘灵感：<>

【灵感池回填】你的选题库延展/灵感池 本周新增：<几条> → 下前置日选选题优先消化
```

> 排周计划时，创作补给块与睡眠/家庭时间同级先占格；疲惫日可缩短不可取消。

## 五、飞书推送（确认后执行）

**先问用户推哪边**：日历（时间块 + 提醒，适合纪律执行）还是多维表格（可追踪/可筛选表，适合长期复盘）。两者命令都带 `--as user`。

### 5.1 飞书日历（已验证命令）

```bash
# 单个时间块事件
lark-cli calendar +create --as user \
  --calendar-id primary \
  --summary "<事件标题>" \
  --start "2026-09-03T09:00:00+08:00" \
  --end   "2026-09-03T10:30:00+08:00" \
  --description @"./时间安排/周计划_2026-W36.md" \
  --json

# 周期性（如每周六晨训）：用 --rrule
lark-cli calendar +create --as user --calendar-id primary \
  --summary "[你的训练名] Y1" \
  --start "2026-09-05T07:00:00+08:00" \
  --end   "2026-09-05T08:00:00+08:00" \
  --rrule "FREQ=WEEKLY;BYDAY=SA;BYHOUR=7;BYMINUTE=0" \
  --json
```

**坑位（复用经验）**
- `--start` / `--end` 用 **ISO 8601 带时区偏移**，如 `2026-09-03T09:00:00+08:00`，别用裸本地时间。
- `--description` 用 `@file` 传，别内联中文引号/反斜杠；也支持 `-` 从 stdin 读。
- `--as user` 必须带（写日历只认 user 身份）。
- 批量排周：把每天每个时间块循环跑 `+create`；或先本地草稿确认无误再批量，避免重复建事件。建完用 `lark-cli calendar +agenda` 抽查。
- 高风险写操作若被拦截需 `--yes`，仅在用户明确确认后加。

### 5.2 飞书多维表格（命令以 lark-base 技能为准）

本 skill 不硬编码 base 建记录语法（CLI 演进快），推送前**先加载 `lark-base` 技能**取当前 `records create` 精确参数。约定：
- 表结构建议字段：`日期 | 时段 | 精力等级 | 任务 | 消耗类型 | 状态(待办/进行/完成) | 备注(⚠️降级原因)`。
- 用 `--base-token <token>` 指定表（flag 是 `--base-token`）。
- 建表可用 `lark-cli base +base-create`；建记录用 `lark-base` 技能里的 `records create` 写法。

## 六、对外物料禁词提醒（联动 course-to-action-notes）

本 skill 产出默认是**内部计划**，不触发禁词自检。但若用户要把这份节奏包装成**对外招生 / 海报 / 展览物料**（例如"我的作息法"训练营），必须复用 `course-to-action-notes` 第六节对应平台的禁词清单过一遍——尤其留意 `时间管理 / 时间系统 / 碎片时间 / 行动力 / 行动驱动 / 意志力 / 目标 / 目标导航 / 目标管理` 等一票否决词，命中即重写。

另：你的选题库战略稿里若含某平台「超级个体 / 一人公司」等一票否决词，这**仅**是该平台提交海报/展览时的限制（你曾因类目重叠被退稿）。你本人的**短视频、直播**直接出镜运作、无此禁忌，选题库表述可照用；只有拿去该平台提交的物料才需跑对应禁词清单。

## 七、示例（锚已就位，套用即出）

- 输入（本周特殊项）：[低能量窗口预估日期] → 若本周含该窗口则标 ⚠️；[你的产品/工作坊开课时间待定 → 候选窗口先占 X 晚/Y 晚/Z 下午]；[若本周为双周 → 排长距离训练]。
- 产出：`时间安排/周计划_2026-W36.md`，直接套二.3 周骨架：[高消耗日]训练、[恢复日]恢复+前置+招募、[拍摄日]下午拍摄、[服务日]上午咨询、[备课日]深度备课；低能量窗口内不排讲课/个案。
- 飞书：确认后推日历（训练/拍摄/咨询/工作坊时间块，低能量窗口留空）+ （可选）训练进多维表格。

## 八、落地到创作者工作台（本地显示层）

用户有一个**本地运行的创作者工作台**（无需账号），正好是时间安排的显示/执行终端。它把本 skill 的产出承接成可看、可勾、可复盘的真实界面——**生成本地草稿确认后，优先把它同步进工作台**，而不是只留在 md。

**定位与路径**
- 安装路径：`[工作台安装目录]/creator-workbench-v1.0.0/creator-workbench-v1.0.0/`（以你本地实际解压路径为准）
- 启动：双击 `Start-Windows.exe`（须与 `01-app` 同级；浏览器自动开本机页面）。
- 数据全在 `02-data/<domain>.json`；读写走本地接口 `GET/POST /api/data/<domain>`，请求体 `{ "data": <对象> }`，**必须带访问密钥**。
- 实例信息（端口 / 密钥）**首次启动后从 `02-data/instance.json` 与 `02-data/access-key.txt` 读取，不要硬编码**；或用 `GET /api/instance` 探活。（下方示例端口 `[PORT]` 与密钥 `[KEY]` 仅为占位，请替换为你本机实际值。）

**数据域 ↔ 本 skill 模块映射**
| 域 | 对象结构 | 装什么 |
|---|---|---|
| `plans` | `{list:[{id,name,type:'weekdays'\|'daily',days:[1..7],color,status,slots,moves}], weekFocus:{<周一日期YYYY-MM-DD>:'本周最该拿下的一项'}}` | **周节律骨架**：每条周期事项一项；`days` 用 1=周一…7=周日，`daily` 为每天。**`weekFocus` 为按周映射（以当周周一日期为 key）**——UI 仿月度横条：`‹ ›` 左右翻周 + 「回到本周」按钮，本周带「本周」高亮标签；每周一自动空出一张新框，翻到的周各自独立存档、互不覆盖、历史保留 |
| `series` | `{list:[{id,name,status,items:[{id,name,stage,next,plat}]}]}` | **短视频 N 条内容线 / 选题库**：每条内容线一个 series，选题作 items |
| `ideas` | `{items:[{id,text,type,stage,createdAt}]}`（`stage:'input'`=①向外获取 / `'digest'`=②内省总结） | **创作灵感补给（=采集收件箱，非沉淀库）**：两段式——向外获取 → 内省总结（含与AI对话）；周复盘 distill 进知识库 |
| `tasks` | `{items:[{id,text,tag,done,date}]}` | 当日具体待办（可带日期，对应日计划） |
| `reviews` | `{imports,next,summary}` | 发布后数据反馈复盘（闭环第 8 步） |
| `planning` | `{year:{label,goal},quarters:[{id,label,focus,months:[ym]}],months:{ym:{label,focus}}}` | **年度规划层级**：年目标→季度重心→每月工作重点（独立平铺 1–12 月、本月高亮）→周/日；周/日链接到 plans/tasks |

**同步动作（确认后执行）**
1. 周节律 → 写 `plans`（[你的周节律各项，如训练/恢复/拍摄/咨询/课程/备课/灵感补给]各一条，带 days/color）。
2. 选题库 N 线 → 写 `series`（每线一个 series，候选选题作 items，`stage:'备稿'`）。
3. 灵感沉淀 → 写 `ideas`（type 用 `内容灵感/产品想法/方法沉淀/生活`）。
4. 本周具体动作 → 可写 `tasks`（带 `date` 指定哪天）。
- 写入走 `POST /api/data/<domain>?key=...`，与界面同一条原子写入路径（自动留 `.bak.json`），安全可逆；工作台界面**刷新后才显示新数据**。
- 配色取值：`#55799d #3090f8 #a77438 #75648f #a45b5b #6f878d`。

**日常打开方式（用户入口）**
- 桌面可建快捷方式：双击 = 启动本地服务 + 自动开浏览器，一步到位，无需进安装目录。
- 固定访问地址（建议存浏览器书签）：`http://127.0.0.1:[PORT]/?key=[KEY]`。端口与密钥以你本机 instance.json 为准。
- 服务**非开机自启**：电脑重启后会停。重开任选其一：① 双击桌面图标；② 直接说"帮我开工作台"由 AI 起服务；③ 手动进安装目录双击 `Start-Windows.exe`。
- AI 手动起服务（后台）：在 `01-app` 目录运行 `python server.py --no-browser`，复用 `02-data/instance.json` 故端口/key 不变。
- 数据全在本地 `02-data`，不上云；建议定期把整个 `02-data` 文件夹备份到云盘/飞书（防硬盘故障）。

**边界**：工作台是用户本机工具，不对外、不接手机；同步是"锦上添花"，本地 md 草稿仍是主交付。若工作台未启动，提示用户先双击 `Start-Windows.exe` 或桌面图标，不要臆造数据。

**年度规划层级（年 → 季 → 月 → 周 → 日）**
- 用户心智模型是分层的：先有**年度目标**（如 [你的年度目标，例如「成为有影响力的知识IP」]）→ 年初拆**季度重心**（Q1–Q4，粗略不细）→ 落**月度要事** → 进工作台已有的**周节律（`plans`）/ 今日事项（`tasks`）** → 落到**某一天**。
- 工作台已新增「00 年度规划」模块承载前三层（年/季/月），周与日直接链接到周期计划 / 今日事项，避免重复录入。月度重点原藏在季度折叠里、易被忽略，后改为**横条式「每月工作重点」——单卡只显示当前月、左右 ‹ › 箭头切换月份、当前月标「本月」高亮、附「回到本月」按钮**，季度折叠内不再重复放月输入框，避免双写。今日事项卡片在列表下方还含**「今日时间表」——按上午 / 下午 / 晚上三个时段粗排当天**，后续可细化；数据随 tasks 域按日期存档（tasks.timeline）。
- `planning` 域默认骨架：`year.label=[年份]`、`year.goal` 建议预填你的年度目标（如「成为有影响力的知识IP」）；`quarters` 4 个（各挂 3 个月）；`months` 12 个（首次渲染自动生成，label=「N月」）。
- 用户在模块里直接填：年目标、各季重心、各月要事；点季度可展开/折叠；月旁的「周计划 / 今日」按钮平滑滚动并高亮对应模块。
- 排周/日计划时，先回看 `planning` 的年/季/月，保证下层动作不偏离年度主线。

## 九、边界（不对用户替决定的事）

- 最终排不排某件事、训练强度取舍、家庭时间如何切分——AI 只排草稿，用户拍板。
- 健康闸门是"建议降级"，不是替用户取消承诺；最终是否执行由用户定。
- 不把草稿直接对外发布；对外须过禁词自检（见第六节）。
